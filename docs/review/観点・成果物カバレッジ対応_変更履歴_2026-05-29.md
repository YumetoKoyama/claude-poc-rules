# 観点・成果物カバレッジレビュー 対応 変更履歴

- 日付: 2026-05-29
- 対象: `CLAUDE.md` および `.claude/skills/` 配下の skill 群（フレームワーク更新）
- 根拠: [観点・成果物カバレッジレビュー_2026-05-29.md](観点・成果物カバレッジレビュー_2026-05-29.md) の **重要度: 中以上 13 件**（高 6 / 中 7）
- 対応方針（ユーザー判断）: **フレームワーク更新**（CLAUDE.md 推奨ドキュメント一式・開発ルール・ID 体系へ登録 + review/produce skill に観点・生成指示を追加）。**作り込み深さは「登録＋観点追加まで」**（専用テンプレート .md の新設・運送マッチング案件固有の中身作成は対象外）。

---

## 1. 追加・登録した成果物と ID（CLAUDE.md）

### 要件定義（docs/requirements/）に登録

| 成果物 | 対応指摘 | 新 ID |
| --- | --- | --- |
| `権限マトリクス.md` | G-01 | （ロール × UC/ENT/SCR の操作可否、テナント越境拒否条件） |
| `メッセージ一覧.md` | R-01 | **MSG-XXX**（+ オープン課題 `Q-MSG*`） |
| `コード値定義.md` | R-02 | （業務区分値の概念正典。`_common.yaml` enum と対応） |
| `通知・文面定義.md` | R-03 | （`メッセージ一覧.md` に統合可） |

### 設計（docs/design/）に登録

| 成果物 | 対応指摘 |
| --- | --- |
| `方式設計.md` | D-02 |
| `セキュリティ設計.md`（認可設計を内包 / `認可設計.md` 分割可） | D-01・G-01 |
| `バッチ設計.md` / `共通部品設計.md` | D-03 |
| `運用設計.md` | D-04 |
| `非機能テスト計画.md` / `セキュリティテスト観点.md` | T-02・T-03 |

### テスト（docs/test/）に登録

| 成果物 | 対応指摘 | 新 ID |
| --- | --- | --- |
| `結合テストマトリクス.md` | T-01 | **IT-XXX** |
| `トレーサビリティマトリクス.md`（RTM） | G-02 | — |
| `単体テストマトリクス.md`（列を明確化） | — | TC-XXX |

> 配置規約を CLAUDE.md に明記: テストの「戦略・計画・観点」は設計フェーズ成果物として `docs/design/`、「ケース化したマトリクス」は製造フェーズ成果物として `docs/test/`。

### 開発ルール・フロー図の追記

- 開発フローの produce 一覧（ステップ 1・3）に新成果物を追記。
- 開発ルールに、要件 4 件・設計 5 件・テスト/横断 4 件の生成ルールを追加。
- ID 凡例ルール（CLAUDE.md 全体定義）に **MSG-XXX**・**IT-XXX** を追加。

---

## 2. review 系 skill の観点追加（loop で機械的に強制）

### `review-requirements/SKILL.md`

- Read 対象に `権限マトリクス.md` / `メッセージ一覧.md` / `コード値定義.md` / `通知・文面定義.md` を追加。
- BLOCK 観点 `authorization` / `message_catalog` / `code_value` を追加（存在しない・空・散在の検出）。
- SUGGEST 観点 `authorization`（テナント越境条件欠落）/ `message_catalog`（区分・文面欠落）を追加。
- `category` enum を更新。

### `review-design/SKILL.md`

- Read 対象に要件 4 成果物 + 設計 8 成果物（方式設計・セキュリティ設計・認可設計・バッチ設計・共通部品設計・運用設計・非機能テスト計画・セキュリティテスト観点）を追加。
- BLOCK 観点 `architecture-doc`（方式設計）/ `security-design`（認証認可の正典 + 認可設計）/ `batch-design` / `operation-design` を追加。
- SUGGEST 観点 `common-component` / `nonfunc-test` / `message-trace`（要件 MSG・コード値・通知文面との相互参照）を追加。

### `review-implementation/SKILL.md`

- BLOCK `security` を **OWASP ベース**（認可バイパス・IDOR/テナント越境・JWT 検証・機密情報のログ/レスポンス出力・入力サニタイズ）に具体化し、`セキュリティテスト観点.md` と対応づけ（G-03）。
- SUGGEST `test_design` に **結合テストマトリクス（IT-XXX）** を追加。新規 SUGGEST `traceability_matrix`（RTM 反映）/ `nonfunc_test`（非機能テスト計画の実施）を追加。

---

## 3. produce 系 skill の生成指示追加

### `requirements-from-input/SKILL.md`

- 成果物一覧・指示（18.1〜18.4）・完了条件に、権限マトリクス / メッセージ一覧（MSG-XXX）/ コード値定義 / 通知・文面定義の生成手順を追加。未確定文言は `Q-MSG*` へ切り出すルールを明記。

### `design-from-requirements/SKILL.md`

- 成果物一覧・指示（9.1〜9.5）・完了条件に、方式設計 / セキュリティ設計（認可設計）/ バッチ設計 / 共通部品設計 / 運用設計 / 非機能テスト計画 / セキュリティテスト観点の生成手順を追加。要件 MSG・コード値・通知文面との相互参照（9.5）を明記。

### `implement-from-issue/SKILL.md`

- 品質ゲート表に **結合テスト**（IT-XXX）行と **セキュリティレビュー**（OWASP・PR 前必須）行を追加（T-01・G-03）。
- 非機能テスト計画の実施と RTM 反映のルールを追加。

---

## 4. 検証結果

- `check-skill-names.sh`: 合格（全 `*-loop` の SlashCommand 参照が実在 skill に解決）。
- 編集した skill 6 ファイルの iconv UTF-8 検証: 全 OK。
- `CLAUDE.md`: 連携フォルダ上の実ファイルを再読込し、全追記が反映され末尾まで欠損なく完結していることを確認（行 200 + 末尾改行）。
- 配置の整合: 「戦略・計画・観点（design）/ マトリクス（test）」の二分を CLAUDE.md・review-design・implement-from-issue で統一。

> 補足: bash の作業用マウントが `CLAUDE.md` の旧コピーを一時的に表示し、切断チェックが誤検知（BLOCK）を出したが、連携フォルダ上の実ファイル（Read で確認）は完全であり、誤検知と判断した。

---

## 5. `implement-from-issue/SKILL.md` の復旧について（解決済み）

本対応中、`.claude/skills/implement-from-issue/SKILL.md` への一括置換コマンドがワークスペースのタイムアウトで中断し、**ファイルが 0 バイトに切り詰められた**。当該ファイルは本リポジトリの git では追跡対象外（untracked）で、復元元のコミットが無かったため、一旦は残存コンテキストから暫定再構築していた。

その後、**ユーザーから置換前のバックアップ（元ファイル）が提供された**ため、暫定再構築版を破棄し、**元ファイルを逐語で復元**したうえで、本対応の意図（重要度: 中以上）に対応する追加分のみを当て直した。元ファイルの本文（GitHub MCP 連携手順・`feature/issue-<N>` ブランチ規約・PR テンプレート・ラベル運用 `status:ready`→`status:in-review` 等）はすべて元のまま保持している。

元ファイルに対して追加した差分は次のとおり（これら以外は無変更）:

- 品質ゲート表に **結合テスト**（`docs/test/結合テストマトリクス.md`・IT-XXX）行と **セキュリティレビュー**（OWASP 観点・PR 前必須・`docs/design/セキュリティテスト観点.md`）行を追加。
- 品質ゲート節に、非機能テスト計画の実施と RTM（`docs/test/トレーサビリティマトリクス.md`）反映のルールを 1 行追加。
- PR テンプレートの「品質チェック結果」に Integration Test / Security Review を追加。
- 最終報告テーブルの品質チェック欄に integration / security を追加。
- 完了条件に「結合テスト・セキュリティレビュー」を追加。

UTF-8 検証 OK。元ファイル（178 行）+ 上記追加で整合済み。
