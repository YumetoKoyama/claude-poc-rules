# 実装スキル 整合ギャップ一覧

> ⚠️ **本書は派生分析（参考）であり正典ではない。** 技術スタックの正典は `rules/`（FE: `frontend-*.md` / BE: `backend-*.md`）。

> アップロードされた frontend ルール 4 本（`rules/frontend-directory-structure.md` / `frontend-routing.md` / `frontend-state-api.md` / `frontend-testing-naming.md`）と、現行の実装スキル群および CLAUDE.md の前提とのギャップを棚卸しした **差分一覧（修正前の検討材料）**。本ドキュメントは修正そのものは行わず、矛盾点・影響・推奨対応のみを記す。

## 前提（確定事項）

- **リポジトリ構成: FE / BE 別リポジトリ**（ユーザー確定）。
- **frontend ルール（あとから流し入れた 4 本）を正典とする**（ユーザー確定）。CLAUDE.md 標準スタックおよび実装スキル群は、矛盾する場合 frontend ルール側に合わせる。
- ただし frontend ルールは**一部修正中で今後変更の可能性がある**。実際のスキル修正はルールが安定してから着手し、本一覧はルール更新に追随して見直す。

## 凡例

| 略号 | 意味 |
| --- | --- |
| G-XX | ギャップ ID（本ドキュメント内連番） |
| 重大度 | 🔴 致命（このままだと製造フローが破綻 / 必ず BLOCK 化）／🟠 重要（実装品質・整合に直結）／🟡 改善（あると望ましい） |
| 対象スキル | `impl-issue`=implement-from-issue / `impl-loop`=implement-loop / `review`=review-implementation / `fix`=fix-implementation / `issues`=create-issues-from-design |

---

## 1. スタック・構成の根本的食い違い

| ID | 観点 | 現行スキル / CLAUDE.md の前提 | frontend ルールの前提 | 矛盾の内容と影響 | 推奨対応 | 重大度 | 対象 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| G-01 | FW・ルーティング | Vite + React Router（CLAUDE.md 標準スタック）。impl-issue 手順4-3「React 画面 / ルーティング」 | Next.js **App Router**（Pages Router 禁止）、route group `(public)`/`(protected)`、`middleware.ts`、`route.ts` | 別 FW を前提にしており、生成されるルーティング・レイアウト構造が全く異なる。設計書の画面遷移→実装の対応も崩れる | CLAUDE.md 標準スタックを Next.js App Router に更新（正典化する場合）。少なくとも impl-issue のフロント手順を FW 非依存記述に直し、ルール参照に委譲 | 🔴 | impl-issue, CLAUDE.md |
| G-02 | ディレクトリ構成 | 「Presentational / Container に分け、API 呼び出しは `src/api/` に集約」 | **機能別（features）構成**。`app/`(薄いエントリ) / `features/*/{components,hooks,services,stores,schemas,types}` / `components`(共通UI) / `lib/api/client.ts` | 分割軸が技術別 vs 機能別で正反対。`src/api/` と `lib/api`+`features/*/services` も別物。impl-issue の指示どおり作るとルール違反コードを量産 | impl-issue 手順4-3を frontend ルール参照に置換（後述 G-09） | 🔴 | impl-issue |
| G-03 | 状態管理 | 「Redux Toolkit または Zustand」（曖昧） | **Zustand 固定**。store は `features/*/stores/*.store.ts`、`src/stores` 集約禁止、サーバーデータの無差別永続化禁止、React Query 検討 | 実装スキルが状態管理の置き場・対象を規定していないため、ルールの禁止事項に抵触する実装になりやすい | impl-issue / review に Zustand 配置・管理対象ルールを反映 | 🟠 | impl-issue, review |
| G-04 | バリデーション・モック | 明示なし（フロントは Vitest + RTL のみ） | **Zod**（schema）、**MSW**（API モック）を採用 | テスト・スキーマ層の標準が抜けており、ルールと不一致な実装が通る | 標準スタック/テスト記述に Zod・MSW を追記 | 🟡 | impl-issue, CLAUDE.md |
| G-05 | ロール分割 | 記述なし | carrier / shipper を URL・route segment・feature で分離、`/forbidden` 統一、共通は `common` へ | 画面 Issue を実装する際のロール境界の落とし込み規約がスキルに無い | impl-issue にロール境界の実装規約を追記（G-08 と関連） | 🟠 | impl-issue |

---

## 2. リポジトリ構成（FE/BE 別）に起因するギャップ

| ID | 観点 | 現行スキルの前提 | 別リポジトリ実態 | 矛盾の内容と影響 | 推奨対応 | 重大度 | 対象 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| G-06 | ブランチ / PR 単位 | impl-issue 手順3〜7：DB→BE→FE を **1 feature ブランチ + 1 PR** で実装。`Closes #N` で 1 Issue を close | FE と BE が別リポジトリ。1 Issue が 2 リポジトリにまたがり得る | 1 ブランチ/1 PR 前提が成立しない。どちらの repo に PR を作るか、両 repo にまたがる Issue をどう close するかが未定義 | impl-issue を「Issue の type / 対象 repo を判定 → 該当 repo で個別にブランチ・PR」に再設計。クロス repo Issue は子 Issue 分割 or PR を 2 本作り相互リンク | 🔴 | impl-issue, issues |
| G-07 | 品質ゲートのレポート同居 | review 手順4：`backend/target/...` と `frontend/coverage/...` を **同一作業ツリー**で更新時刻判定 | 別 repo のため両レポートが同一チェックアウトに同居しない | review の「固定パスの更新時刻で実行有無を判定」が片 repo でしか機能しない | レポート判定を repo 単位に分離（BE repo では BE ゲート、FE repo では FE ゲートのみ確認）。impl-issue 手順5の固定パス表も repo 別に再編 | 🟠 | impl-issue, review |
| G-08 | Issue→実装の落とし込み | Issue は 画面/API/IF/table 単位で起票（issues） | FE は機能別（carrier/shipments 等）、BE は別 repo | 画面 Issue が「app/ エントリ + features スライス」のどこに落ちるか、API Issue がどちらの repo に紐づくかの規約が無い | issues / impl-issue に「Issue type × 対象 repo × FE 機能スライス」の対応規約を追加 | 🟠 | issues, impl-issue |

---

## 3. テスト（E2E）の矛盾

| ID | 観点 | 現行スキルの前提 | frontend ルールの前提 | 矛盾の内容と影響 | 推奨対応 | 重大度 | 対象 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| G-09 | FE の E2E | impl-issue 手順5：**E2E（Playwright MCP）を必須品質ゲート**化。review：E2E 失敗を **BLOCK**（category=quality_gate） | 「本リポジトリに **e2e 専用ディレクトリを含めない**」「疎通・業務シナリオは AWS デプロイ後の別工程」 | FE repo では実施しない E2E を毎回要求し、review が恒常的に BLOCK / ESCALATE する。ループが収束しない | FE repo の品質ゲートから E2E を外す（BE repo / 結合は別途）。E2E は「AWS デプロイ後工程」として CLAUDE.md・テスト戦略側に移設。review の BLOCK 条件を repo 別に分岐 | ✅ **対応済み**（E2E をゲートから除外・e2e-from-design 凍結） | impl-issue, review, CLAUDE.md |
| G-10 | FE 結合テスト | impl-issue 手順5に「結合テスト（TestContainers 等・実 DB）」 | FE 側は「画面と feature の結合 / API モック(MSW)を使った表示分岐」を単体近傍で実施。実 DB 結合は対象外 | FE repo に実 DB 結合を求めると不整合。FE の「結合寄りテスト」は MSW ベースで別概念 | 結合テストを「BE repo=実 DB 結合(IT)」「FE repo=MSW 結合(コンポーネント結合)」に分けて定義 | 🟠 | impl-issue, review |
| G-11 | カバレッジ閾値 | review BLOCK：**バックエンド** < 80%。FE は手順5表に「80% 以上」と書くが review BLOCK 条件はBE のみ言及 | FE 単体対象（UI/hook/store/フォーマッタ/バリデーション/APIラッパ）は明確 | FE カバレッジの合格ラインが review の BLOCK 判定に落ちていない（実質ノーゲート） | review に FE カバレッジ（Istanbul）の BLOCK 閾値を明記 | 🟡 | review |

---

## 4. review / fix の観点不足（FE 規約が落ちていない）

| ID | 観点 | 現行 review の観点 | frontend ルールの禁止事項 | 不足内容 | 推奨対応（review カテゴリ追加） | 重大度 |
| --- | --- | --- | --- | --- | --- | --- |
| G-12 | フロント構成違反 | architecture は「Controller に業務ロジック / フロントに業務判定」止まり | `src/stores` 集約禁止 / 業務 hook の `src/hooks` 集約禁止 / `components` への業務依存 UI 大量配置禁止 / feature 循環参照禁止 / role 直下の巨大 hooks・stores 禁止 | FE の構成規約違反を検出する観点が無い | review に `frontend_convention`（BLOCK/SUGGEST）を新設し、上記を機械的にチェック | 🟠 |
| G-13 | データ取得の置き場 | なし | `page.tsx` 内 fetch 禁止 / UI で直 fetch 禁止 / URL 直書き禁止 / Route Handler 単純中継の乱用禁止 / Zustand への無差別永続化禁止 | API 連携の境界違反を検出できない | 同上カテゴリで Grep ベース検出（`fetch(` in page.tsx 等） | 🟠 |
| G-14 | 3 層認可の検証 | security は OWASP/BE 寄り（IDOR・JWT・テナント越境等） | 認可は middleware / protected layout / page+feature の **3 層**に分担、「1 箇所集約禁止」 | FE 側の認可分担（middleware だけで全部やる等）を検証する観点が無い | security 観点に「FE 認可 3 層の分担」チェックを追加。要件 `権限マトリクス.md`・設計 `認可設計.md` と突合 | 🟠 |
| G-15 | ルール参照の欠如 | スキルは frontend ルールを一切読み込まない | 4 本のルールが Source of Truth になり得る | FE 実装・レビュー時にルールが参照されないため、規約が形骸化 | ルール 4 本を `docs/design/frontend/`（または `.claude/skills/_common/references/frontend/`）に正式配置し、impl-issue / review / fix が対象が FE のとき必ず Read する手順を追加 | 🔴 |
| G-16 | 命名・接尾辞 | なし | `.store.ts` / `.service.ts` / `.schema.ts` / `.test.ts` / PascalCase コンポーネント、kebab-case ディレクトリ | 命名規約違反を検出しない | review `frontend_convention` の SUGGEST/NIT に命名チェックを追加 | 🟡 |

---

## 5. 上流（CLAUDE.md・設計フェーズ）への波及

| ID | 内容 | 推奨対応 | 重大度 |
| --- | --- | --- | --- |
| G-17 | CLAUDE.md「要件未指定時の標準スタック」が Vite/React Router/Material UI のまま | frontend ルールを正典化するなら Next.js App Router / Zustand / Zod / MSW / 機能別構成へ更新。E2E は「FE repo 外（AWS デプロイ後）」と明記 | 🔴 |
| G-18 | テスト戦略・非機能/セキュリティテスト計画が「FE repo に E2E あり」前提 | `docs/design/テスト戦略.md` 等で E2E の所在を「デプロイ後別工程」に整理し、FE repo のテスト 3 層（単体 / MSW 結合 / 命名）を再定義 | 🟠 |
| G-19 | implement-from-design（非推奨）含め、フロント実装規約の Source of Truth が分散 | frontend ルールの配置場所と参照経路を 1 本化し、CLAUDE.md の「ドキュメントの正典」章に追記 | 🟡 |

---

## 推奨する着手順（修正フェーズに進む場合）

1. **G-17 / G-15**: frontend ルールの配置場所を確定し、CLAUDE.md 標準スタックと正典関係を更新（上流の意思決定）。
2. **G-06 / G-07**: 別リポジトリ前提で impl-issue のブランチ・PR を再設計（G-09 の E2E 除外は対応済み）。
3. **G-01〜G-05 / G-08**: impl-issue のフロント実装手順をルール参照に置換し、ロール境界・機能スライスの落とし込みを規約化。
4. **G-12〜G-16**: review に `frontend_convention` カテゴリと FE 認可 3 層・カバレッジ閾値を追加、fix が対応できるようにする。
5. **G-18**: テスト戦略ドキュメントを repo 別・工程別に再整理。

> 🔴 残 4 件（G-01, G-02, G-06, G-15, G-17 のうち G-09 は対応済み）は、修正しないと製造フローが破綻するか恒常 BLOCK を招くため最優先。
