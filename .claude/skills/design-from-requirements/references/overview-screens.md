## 概要.md

```markdown
# 設計サマリー

## 1. スコープ要約（要件定義からの引き継ぎ）

- 参照: `docs/requirements/概要.md`

## 2. 前提条件と未解決事項

### 前提条件

### 未解決事項

| # | 事項 | 影響範囲 | 対応期限 |
|---|------|---------|---------|
|   |      |         |         |

## 3. ユースケースと主要ユーザーフロー

## 4. リスクと影響範囲

## 5. レビュー観点

## 6. 後続工程（人手レビュー・採択後に起動）

- [ ] implement-from-design
- [ ] unit-test-from-design
- [ ] e2e-from-design
```

---

## screens/画面遷移.md

```markdown
# 画面遷移図

要件定義の `docs/requirements/画面一覧.md` の画面 ID 一覧をもとに、画面遷移を Mermaid で表現する。
ノードは画面 ID（SCR-XXX）を使い、ラベルに画面名を含める。

## 全体遷移

\`\`\`mermaid
flowchart LR
    SCR001["SCR-001 ログイン"]
    SCR002["SCR-002 ダッシュボード"]
    SCR003["SCR-003 一覧"]
    SCR004["SCR-004 詳細"]

    SCR001 -->|ログイン成功| SCR002
    SCR001 -->|認証失敗| SCR001
    SCR002 -->|メニュー選択| SCR003
    SCR003 -->|行クリック| SCR004
    SCR004 -->|戻る| SCR003
\`\`\`

## 業務フローごとの遷移（任意）

業務フローが複雑な場合は、業務単位で Mermaid 図を追加する。
```

---

## screens/共通レイアウト.md

> 全画面に共通するアプリシェル（ヘッダー／フッター／グローバルナビ／サイドバー等）を 1 枚で設計する。各画面 md は本ファイルを参照し差分のみ記述する。共通シェルに乗らない画面（ログイン等の認証前画面）は「認証前独立レイアウト」として区分を明記する。FE 実装方式（共通コンポーネント・状態管理）は `フロントエンド共通設計.md` に委ねる。

```markdown
# 共通レイアウト設計

## ID 凡例

| ID | 意味 |
|----|------|
| SCR-XXX | 画面 ID |
| (operationId) | API 操作 ID（`api/*.yaml`） |

## レイアウト区分

| 区分 | 説明 | 適用画面 |
|------|------|---------|
| 共通シェル | ヘッダー＋（サイドバー）＋メインの共通枠 | SCR-002, SCR-003, ... |
| 認証前独立レイアウト | 共通シェルに乗らない（ログイン・パスワードリセット等） | SCR-001, ... |

## 共通シェル構成

\`\`\`mermaid
flowchart TB
    Header["ヘッダー (AppHeader)"]
    Nav["グローバルナビ / サイドバー"]
    Main["メインコンテンツ (各 SCR)"]
    Footer["フッター"]
    Header --> Main
    Nav --> Main
    Main --> Footer
\`\`\`

## 共通シェルの構成要素

> ヘッダー等に表示する項目は **必ずデータ源（operationId.フィールド）を明示**する（共通シェル項目は本ファイルで一括管理し、画面ごとに重複させない）。ログイン直後の取得経路はログインシーケンス（`sequences/*.md`）と整合させる。

| 要素 | 表示内容 | データ源（operationId.フィールド） | 表示条件（ロール等） | 挙動・遷移 |
|------|---------|--------------------------------|------------------|-----------|
| ヘッダー: アプリ名/ロゴ | 固定 | — | 全ロール | クリックでダッシュボード(SCR-XXX) |
| ヘッダー: ユーザー名 | ログインユーザー名 | `login.user.displayName` / `me.displayName` | 認証済 | — |
| ヘッダー: テナント名 | 所属テナント名 | `me.tenantName` | 認証済 | — |
| ヘッダー: ユーザーメニュー | プロフィール/ログアウト | — | 認証済 | ログアウト → 認証破棄 → SCR-001 |
| グローバルナビ | メニュー項目 | 静的 or `me.roles` で出し分け | ロール依存 | 各 SCR へ遷移 |
| 通知ベル（任意） | 未読件数 | `notifications.unreadCount` | 認証済 | 通知一覧へ |

## 共通の挙動

- 認証切れ（401）時のリダイレクト先（例: SCR-001 ログイン）と再ログイン後の復帰方針。
- 権限により非表示にするナビ項目（要件 `権限マトリクス.md` と整合）。
- グローバルエラー表示（トースト等）の表示位置。実装方式は `フロントエンド共通設計.md` に従う。
```

---

## screens/[画面名].md

ファイル名規約: `[SCR-ID]-[画面名].md`（例: `SCR-001-ログイン.md`、`SCR-003-アイテム一覧.md`）。SCR-ID は大文字、画面名は業務用語の日本語で記述する。

```markdown
# 画面設計: SCR-001 ログイン画面

## 画面概要

- 画面 ID: SCR-001
- URL パス:
- 対応ユースケース:
- 関連機能要件: `docs/requirements/functional/[機能名].md`　※**必須・機械可読**。この画面の AC が定義されている機能要件ファイルを `functional/〜.md` 形式で必ず 1 件以上記載する（AC-XXX は機能スコープ採番のため、この参照が無いと画面の AC がどの機能の AC か機械確定できない。`generate-rtm-skeleton.sh` が本欄を読んで RTM 本表の SCR / API 列を自動転記し、`check-wiring-fields.sh` が欠落を SUGGEST で指摘する）
- 所属レイアウト: （共通シェル / 認証前独立レイアウト のいずれか。共通シェルの場合は構成・表示項目を `screens/共通レイアウト.md` に委ね、本画面では差分のみ記述する）

## 入力

> 【P-12・2026-07-02 改善】入力の全項目に **送信先（operationId.リクエストフィールド）** を必須で記載する。送信先の無い入力項目は設計できない（先に API のリクエストスキーマへ反映してから埋める）。画面内完結の項目（検索フィルタのローカル状態等）は「画面内」と明記する。この列は `check-wiring-fields.sh` が機械突合する（registerTenant への項目追加漏れ＝design-amendment の再発防止）。

| 項目名 | 型 | 必須 | 送信先（operationId.フィールド） | 説明 |
|-------|----|------|--------------------------|------|
|       |    |      |                          |      |

## 出力・表示内容

> 表示する全項目および業務判定に使う ID（自他判定・テナント判定・権限判定用）には **データ源（operationId.フィールド）** を必須で記載する。供給元 API が無い項目はこの画面では設計できない（先に API 設計へ反映してから埋める）。ログイン直後の画面・共通ヘッダーの項目は `LoginResponse` または `/me` 相当から取得経路を示す。

| 項目名 | 型 | データ源（operationId.フィールド） | 説明 |
|-------|----|--------------------------|------|
|       |    |                          |      |

## バリデーションメッセージ

| 項目名 | 条件 | メッセージ |
|-------|------|-----------|
|       |      |           |

## 画面遷移（この画面から）

| トリガー | 遷移先（画面 ID） | 条件 |
|---------|--------------|------|
|         | SCR-XXX      |      |

## API 対応

| operationId | HTTP メソッド | パス | 概要 |
|-------------|-------------|------|------|
| [operationId] | GET/POST/PUT/DELETE | /api/[resource] | [概要] |

## セキュリティ考慮事項

- 認証: （要/不要）
- 認可: （ロール・権限）
- CSRF 対策:
- その他:

## 受け入れ条件

> 対応する `docs/requirements/functional/[機能名].md` の AC-XXX を **Given/When/Then 形式のまま**引き継ぐ。要約・圧縮禁止。境界値・権限境界も含める。
>
> **形式厳守**: 下記の 7 列 Markdown テーブル以外の形式（箇条書き `* Given: ...`・番号リスト・見出し分割等）で受け入れ条件を記述してはならない。1 行 = 1 AC。Given/When/Then はそれぞれ独立したセルに収める。

| AC-ID | 区分 | Given（前提状態） | When（操作） | Then（期待結果） | 関連 BR | operationId |
|-------|------|-----------------|------------|----------------|--------|-------------|
| AC-001 | 正常系 | [前提条件（認証状態・データ状態）] | [ユーザー操作または API 呼び出し] | [期待レスポンス・状態変化・メッセージ] | BR-XXX | [operationId] |
| AC-101 | 異常系 | [異常状態の前提] | [操作] | [エラーレスポンス・MSG-XXX] | — | [operationId] |
| AC-201 | 境界値 | [境界条件の前提] | [境界値入力] | [期待結果] | BR-XXX | [operationId] |
| AC-301 | 権限境界 | [ロール・テナント条件] | [操作] | [403/404 またはアクセス許可] | — | [operationId] |
```

---

## フロントエンド共通設計.md

> FE の横断的関心事（共通 UI コンポーネント・API クライアント・エラー表示・共通バリデーション・認可ガード・状態管理方針）を設計する。技術スタックの具体（FW・ライブラリ・バージョン）は再掲せず `claude-poc-frontend/.claude/rules/frontend-*.md`（正典）を参照する。界面契約（ErrorResponse・コード値）は `api/_common.yaml`・要件 `コード値定義.md` を正典とし、FE はそれに対応づけるのみで再定義しない。

```markdown
# フロントエンド共通設計

## 共通 UI コンポーネント

| コンポーネント | 役割 | 主な props / 状態 | 使用画面 |
|--------------|------|-----------------|---------|
| Button / Form / Input | 入力共通 | ... | 全画面 |
| Table / Pagination | 一覧表示 | `PageMeta`（`_common.yaml`）に対応 | 一覧系 SCR |
| Modal / Toast | 確認・通知 | ... | 全画面 |

## API クライアント

- 共通クライアントの責務: ベース URL・JWT 付与・共通ヘッダ・タイムアウト・リトライ方針。
- 認証トークンの保持・更新（リフレッシュ）方針は `セキュリティ設計.md` の JWT ライフサイクルと整合させる。
- 401（認証切れ）/ 403（権限不足）/ 404（越境）のハンドリング方針（リダイレクト・トースト・画面内エラー）。

## エラー表示マッピング（界面契約 → 表示）

> 正典は `api/_common.yaml` の `ErrorResponse`（`code` / `details[].reason`）と要件 `メッセージ一覧.md`（MSG-XXX）。FE はコードを表示文言へ対応づけるのみで、コード体系・文言を新規定義しない。

| ErrorResponse.code | 対応 MSG-XXX | 表示方法（トースト/フィールド） |
|--------------------|-------------|------------------------------|
| VALIDATION_ERROR | MSG-XXX | フィールド直下に `details[].reason` を表示 |
| FORBIDDEN | MSG-XXX | トースト |
| NOT_FOUND | MSG-XXX | 画面内エラー |

## 共通バリデーション

- クライアント側バリデーションの範囲（即時フィードバック）と、サーバ側を正とする方針。
- 文字数・必須・enum は API YAML（`required`/`maxLength`/enum）・要件 `コード値定義.md` と一致させる（二重定義しない）。

## ルーティング・認可ガード

- 認証ガード（未認証 → SCR-001）/ ロール別ガード（要件 `権限マトリクス.md` と整合）。
- 共通シェル（`screens/共通レイアウト.md`）に乗せる画面と認証前画面のルート構成。

## 状態管理方針

- グローバル状態（認証ユーザー・テナント・通知）とローカル状態の切り分け。具体ライブラリは `claude-poc-frontend/.claude/rules/frontend-*.md` を参照。
```
