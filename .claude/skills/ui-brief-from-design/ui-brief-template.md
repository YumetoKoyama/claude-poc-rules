# UI 設計ブリーフ テンプレート

UI 設計ブリーフは **2層構造** で作成する:

1. **共通ブリーフ**: `docs/design/ui-design/brief/_共通.md`
   - 全画面横断の DS・共通コンポーネント・ロール・トーン・状態規約・レスポンシブ方針
   - Claude Design に **最初に** 投入してデザインシステムを確立する
2. **画面別ブリーフ**: `docs/design/ui-design/brief/[scr-id]-[画面名].md`
   - 共通ブリーフからの **差分のみ** を記述
   - Claude Design には共通ブリーフ確立後に **まとめて添付** する

すべての項目に `参照:` で出典（設計 md / 要件 md）を必ず付ける。設計書に書かれていない事項は **新たに決めず**、最終節の「未確定事項」に逃がす。

### 略号・ID 採用時の凡例必須ルール

CLAUDE.md の開発ルールに従い、UI 設計ブリーフでも **ID または略号を導入する場合は、当該ドキュメントの冒頭付近に凡例を必ず出力する**。`_共通.md` でロール略号やコンポーネント略号（例: `CMP-001`、`Role-A`/`Role-B` 等）を新規導入した場合は `_共通.md` 冒頭の凡例セクションに掲載し、画面別ブリーフはその凡例を参照する形にする（画面別ブリーフ側で新規略号を導入しない）。

引用のみで導入しない既知 ID（`SCR-XXX`、`AC-XXX` 等）については、出典 md 側の凡例を `参照:` で示せばよい。

---

## A. 共通ブリーフテンプレ（`_共通.md`）

```markdown
# UI 設計ブリーフ（共通）

- 対応する設計概要: `docs/design/概要.md`
- 対応するブランドガイドライン: `docs/requirements/ブランドガイドライン.md`
- 対応する画面遷移: `docs/design/screens/画面遷移.md`

> このファイルは Claude Design に **最初に** 投入する共通ブリーフ。デザインシステム・共通コンポーネント・ロール・トーンの基盤を確立する。画面個別の事項は `[scr-id]-*.md` に記述する。

---

## 1. プロダクト概要

- システム名:
- 1〜2 段落の概要:
- 主要ユーザー種別（ロール）:
- 参照: `docs/design/概要.md`、`docs/requirements/概要.md`

## 2. ロール / チャネル

| ロール | 内部識別子 | アクセスできる画面 ID 群 |
|------|-----------|----------------------|
|      |          |                      |

- 参照: `docs/requirements/業務ルール.md`、`docs/design/概要.md`

## 3. トーン & ボイス

- 言語:（例: 日本語固定）
- 敬体 / 常体:
- 絵文字使用可否:
- ボタン命名規則:（例: 体言止め）
- マイクロコピー方針:
- 参照: `docs/requirements/ブランドガイドライン.md` の「トーン & ボイス」

## 4. Design System

### 4.1 カラーパレット

| 用途 | 論理名 | 使い所 |
|------|------|------|
| プライマリ |  |  |
| セカンダリ |  |  |
| アクセント |  |  |
| 警告 / Danger |  |  |
| Info |  |  |
| Success |  |  |
| 背景 |  |  |
| サーフェス（カード） |  |  |
| テキスト（強） |  |  |
| テキスト（弱） |  |  |
| 罫線（弱） |  |  |
| 罫線（標準） |  |  |

### 4.2 タイポグラフィ

- フォントファミリ:（Display / Body / Mono）
- スケール (px): 例 `12 / 13 / 14 / 16 / 18 / 20 / 24 / 30 / 36 / 48`
- 行高: body / heading / display
- 用途別マッピング: H1 / H2 / 本文 / ラベル / 監査ログ

### 4.3 スペーシング・角丸・シャドウ・フォーカスリング

- スペーシングスケール:
- 角丸: Button / Input / Card / Modal / Pill
- シャドウ: xs / sm / md / lg の用途
- フォーカスリング: 太さ・色・オフセット

### 4.4 アイコン

- ライブラリ:（例: Lucide outline, 1.5px stroke）
- 使用許可リスト:
- 絵文字 UI クローム使用可否:

- 参照: `docs/requirements/ブランドガイドライン.md` の「ビジュアルガイドライン」

## 5. 共通コンポーネント

各コンポーネントの寸法・状態・振る舞いを規約として定義する。画面別ブリーフではこれを参照するのみとし、再掲しない。

- **AppHeader**: 高さ / 左右レイアウト / 検索バー / 通知 / ユーザーメニュー
- **SideNav**: 幅 / 役割別表示 / アクティブ状態 / ログアウト
- **Button**: variant (primary / secondary / ghost / danger) / size / 状態 (hover / press / disabled / loading)
- **Field**: ラベル位置 / 必須印 / hint / error 表示
- **Modal**: スクリム / 角丸 / 既定幅 / 不可逆操作の表現
- **Table**: 列幅規約 / zebra / sort / pagination / 空・loading・error 状態
- **StatusBadge / Tag**: ステータス用 vs 一般用、配色マッピング
- **Card / Panel**: padding / shadow / border / hover
- **Pagination / Breadcrumbs / Toast**:

- 参照: `docs/requirements/ブランドガイドライン.md` の「コンポーネント・パターン方針」

## 6. 状態表現規約（States）

| 状態 | 表示 | 既定コピー |
|------|------|---------|
| 空データ |  | 該当するデータはありません |
| Loading |  | （スピナー / スケルトン） |
| エラー（一般） |  | エラーが発生しました。時間をおいて再度お試しください |
| 権限不足 |  | この操作を実行する権限がありません |

- 参照: `docs/requirements/業務ルール.md`、`docs/design/概要.md`「エラーメッセージ共通仕様」

## 7. レスポンシブ方針

- 想定デバイス優先度:
- ブレークポイント:
- 最大コンテンツ幅:
- グリッド:
- 参照: `docs/requirements/ブランドガイドライン.md` の「想定デバイスとレスポンシブ方針」

## 8. アクセシビリティ

- 準拠レベル:（例: WCAG 2.1 AA）
- キーボード操作:
- スクリーンリーダー考慮:
- カラーコントラスト:
- 参照: `docs/requirements/非機能要件.md`

## 9. アニメーション / モーション

- 既定 easing / duration:
- ホバー時の挙動規約:（スケール変化禁止など）
- モーダル開閉のフェード:
- 禁止事項:
- 参照: `docs/requirements/ブランドガイドライン.md`

## 10. 未確定事項（全体）

| # | 事項 | 推奨アクション |
|---|------|-------------|
|   |      |             |

---

## 11. Claude Design 投入用プロンプト（共通投入時）

> 下記をそのまま Claude Design のチャットに貼り付け、本 `_共通.md` を添付する。画面別ブリーフはまだ添付しない。

```
このプロダクトのデザインシステムと共通コンポーネントを確立してください。
添付の _共通.md にロール・トーン・カラーパレット・タイポグラフィ・共通コンポーネント・
状態規約・レスポンシブ方針・アクセシビリティが定義されています。

これに従ってデザイントークン（CSS 変数）と共通プリミティブ
（AppHeader / SideNav / Button / Field / Modal / Table / StatusBadge / Tag / Card / Panel）
の方針を提案してください。画面別の生成は次のステップで依頼します。
```
```

---

## B. 画面別ブリーフテンプレ（`[scr-id]-[画面名].md`）

```markdown
# UI 設計ブリーフ: SCR-001 ログイン画面

- 画面 ID: SCR-001
- 対応する設計 md: `docs/design/screens/SCR-001-ログイン.md`
- 対応する機能要件: `docs/requirements/functional/ログイン.md`
- 共通ブリーフ: `docs/design/ui-design/brief/_共通.md`（DS・共通コンポーネント・トーンは共通ブリーフを参照）

> このファイルは **共通ブリーフからの差分のみ** を記述する。DS や共通コンポーネントの再掲は不要。

---

## 1. Goal（この画面の目的）

- 一次目的:
- 二次目的:
- 成功指標（あれば）:
- 参照: `docs/design/screens/SCR-001-ログイン.md` の「画面概要」「対応ユースケース」

## 2. Audience（差分）

> 共通ブリーフの想定ロールから絞られる場合のみ記述。全ロール向けなら「共通ブリーフに従う」と書く。

- ロール:（例: 未認証ユーザー / SHIPPER のみ / CARRIER のみ）
- 参照: `docs/requirements/業務ルール.md`

## 3. Content（表示する情報・固有コピー）

| 領域 | 表示要素 | 出典 |
|------|---------|------|
| メイン |         | 設計 md「入力」「出力・表示内容」 |
| 補助領域 |         |        |
| 画面固有の空データ時メッセージ | | |
| 画面固有のエラーメッセージ |   | 設計 md「バリデーションメッセージ」 |

- 参照: `docs/design/screens/SCR-001-ログイン.md`

## 4. Layout（領域分割と情報階層）

- 主要レイアウト:（例: 中央寄せの 1 カラム、左フィルタ + メインテーブル）
- ファーストビューに必須の要素:
- スクロールで現れてよい要素:
- 情報の重要度順序（1 → n）:
- 共通シェル（AppHeader / SideNav）の上に乗せるか、認証前の独立レイアウトか:
- 参照: 設計 md「画面概要」

## 5. States（差分）

> 共通ブリーフの「状態表現規約」から逸脱する/追加で必要な状態のみ記述。

| 状態 | 振る舞い | 表示要素 |
|------|---------|---------|
|      |         |        |

- 参照: 設計 md「バリデーションメッセージ」+ 業務ルール BR-XXX

## 6. Interaction（操作と遷移）

- 主要ボタン / リンクの挙動:
- フォーム入力中のリアルタイムバリデーション方針:
- 確認モーダル / トーストの使い分け:
- キーボード操作（Tab 順序 / Enter / Esc）:
- 参照: 設計 md「画面遷移（この画面から）」「セキュリティ考慮事項」

## 7. 画面固有のバリデーション

| フィールド | 制約 | エラーメッセージ |
|---------|------|--------------|
|         |      |              |

- 参照: 設計 md「バリデーションメッセージ」

## 8. 画面遷移（この画面から）

| 操作 | 遷移先画面 ID | 備考 |
|------|------------|------|
|      |            |      |

- 参照: `docs/design/screens/画面遷移.md`、設計 md「画面遷移」

## 9. 強調したい点（任意）

> Claude Design に特に注意してほしい点を箇条書きで。なければ省略可。

-

## 10. 未確定事項

| # | 事項 | 推奨アクション |
|---|------|-------------|
|   |      | 設計フェーズへ差し戻し / Claude Design で対話的に決める |
```

---

## ファイル名規約

`[SCR-ID]-[画面名].md`（例: `SCR-001-ログイン.md`、`SCR-003-アイテム一覧.md`）。SCR-ID は大文字、画面名は業務用語の日本語で記述する。
対応する設計 md と同じファイル名を使い、相互参照を容易にする。

共通ブリーフは `_共通.md` 固定（先頭アンダースコアでソート上位に来るため、目視で「共通」と分かる）。

---

## README.md（索引）

`docs/design/ui-design/brief/README.md` には以下を含めること。

```markdown
# UI 設計ブリーフ索引

`docs/design/screens/` の各画面について、Claude Design 投入用のブリーフをここに置く。

## ファイル構成

```
docs/design/ui-design/brief/
├── README.md              ← この索引
├── _共通.md               ← 共通ブリーフ（Claude Design に最初に投入）
├── SCR-001-ログイン.md
├── SCR-010-アカウント登録.md
└── ...                    ← 画面ごとに 1 ファイル（共通ブリーフからの差分のみ）
```

## 画面 ID とブリーフの対応

| 画面 ID | 画面名 | ブリーフ | 状態 |
|--------|------|---------|------|
| SCR-001 | ログイン | [SCR-001-ログイン.md](SCR-001-ログイン.md) | draft / 採択済み |

## Claude Design 投入手順（順序固定）

1. claude.ai/design でプロジェクトを新規作成する。
2. **最初に `_共通.md` を添付** し、その末尾の「Claude Design 投入用プロンプト（共通投入時）」をチャットに貼る。Claude Design がデザインシステム方針を提案するまで待つ。
3. **画面別ブリーフ（`SCR-*.md`）を一括で（または役割/カテゴリ単位で）添付** し、「これらの画面を上記 DS で生成してください」と依頼する。
4. 生成結果を確認し、必要に応じて対話で調整する。
5. Export → **Handoff bundle** を取得し、**Export 構造そのまま** `docs/design/ui-design/handoff/` 配下に格納する（このディレクトリ作成と格納は人手で実施）。
6. handoff bundle の `README.md` にある画面 ID → prototype 関数のマッピング表が、後続の `create-issues-from-design` の入力となる。

## ui-design/handoff の格納ルール（重要）

Claude Design の Export 物は **Export 構造そのまま** で格納すること。画面単位 (`[scr-id]/`) で切り分けない。

```
docs/design/ui-design/handoff/
├── README.md              ← Export の README（scr-id → prototype 関数のマッピング表を含む）
├── prototype/
│   ├── index.html
│   ├── wf-primitives.jsx       ← 共通プリミティブ（横断的共有資産）
│   ├── wf-shell.jsx            ← AppHeader / SideNav（横断的共有資産）
│   ├── wf-screens-auth.jsx     ← 認証系画面群（複数 SCR が同居）
│   ├── wf-screens-shipper.jsx
│   └── ...                     ← カテゴリ単位の jsx
└── tokens/
    ├── colors_and_type.css
    └── DESIGN_SYSTEM_GUIDE.md
```

prototype/ 配下を人手で画面単位に分割してはいけない。共通プリミティブ・シェル・トークンは横断的共有資産であり、画面 ID 単位に分割すると同期地獄になる。画面 ID と prototype 関数の対応は `ui-design/handoff/README.md` のマッピング表が Source of Truth。

## デザインシステムの優先順位

- Claude Design 側に組織デザインシステムが構築されている場合 → そちらを優先。
- 構築されていない、または上書きが必要な場合 → `_共通.md` の値を共通ブリーフ投入時に明示し、Claude Design にも貼り込む。

## 要件定義側へのフィードバック

`ブランドガイドライン.md` が未整備のため `_共通.md` が埋まらない項目があった場合、ここに列挙して
`docs/requirements/オープン課題.md` への追記を促す。
```
